local function calc_cost(size, rate) return math.ceil(size*size*rate) end

-- Claim, unclaim, add/remove players, myclaims (same as previous version)
-- Copy the full commands.lua logic from previous modular version
